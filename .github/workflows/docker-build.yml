name: Docker Image
on:
  schedule:
    - cron: '0 0 * * 0'  # Weekly on Sunday
  push:
    paths: ['docker/**', 'bench/**']
  workflow_dispatch:

env:
  REGISTRY_IMAGE: ghcr.io/${{ github.repository }}

jobs:
  build-image:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v4

      - name: Build and push build image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile
          target: browser-bench-build
          push: true
          tags: ${{ env.REGISTRY_IMAGE }}/browser-bench-build:latest
          cache-from: type=registry,ref=${{ env.REGISTRY_IMAGE }}/browser-bench-build:cache
          cache-to: type=registry,ref=${{ env.REGISTRY_IMAGE }}/browser-bench-build:cache,mode=max

  measure-image:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@v6

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v4

      - name: Build and push measure image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: docker/Dockerfile
          target: browser-bench-measure
          push: true
          tags: ${{ env.REGISTRY_IMAGE }}/browser-bench-measure:latest
          cache-from: type=registry,ref=${{ env.REGISTRY_IMAGE }}/browser-bench-measure:cache
          cache-to: type=registry,ref=${{ env.REGISTRY_IMAGE }}/browser-bench-measure:cache,mode=max
